# Execution

### To run backend locally, just use Docker:

`docker-compose up --build`

### To run frontend (node 18 required):

### On Linux:

```
cd ./app
npm install
chmod +x startapp.sh
./startapp.sh
```
